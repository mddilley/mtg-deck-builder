<h3>Deck Name: <%= @deck.name %></h3>

<p>Deck Color(s): <%= eval(@deck.color).join(" / ") %></p>

<p>Deck Size: <%= @deck.size %> cards (<%= @deck.size.to_i - @deck.cards.size %> cards remaining)</p>

<h3>Cards in Deck</h3>

<% if @deck.cards.count == 0 %>

<p><%= "Please select 'Edit Deck' to add cards to this deck." %></p>

<% else %>

  <% @deck.cards.uniq.each_with_index do |card, i| %>

  <p><%= i + 1 %>. <a href="/cards/<%= card.id %>"><%= card.name %></a>  x <%= @deck.cards.select {|c| c.name == card.name}.size %></p>

  <% end %>

<% end %>

<p></p>

<a href="/decks/<%= @deck.id %>/edit"><button type="button" name="button">Edit Deck</button></a>
<p></p>
<form action="/decks/<%=@deck.id%>" method="post">
<input type="hidden" id="hidden" name="_method" value="delete">
<input type="submit" value="Delete Deck" id="Delete Deck">
</form>
